generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Student {
  id        String @id
  name      String
  batchYear Int
  address   String
  createdAt DateTime @default(now())
}
